#include <iostream>
#include <vector> // Like Python's lists. Standard library

using namespace std; // std:: can be omitted using this.

int main() {
    vector<int> intvectorvariable;
    int intvariable = 5;
    cout << intvariable << endl;
    return 0;
}